# Usando Python (com a biblioteca pandas)

```shell
pip install pandas openpyxl
````
> [!IMPORTANT]
> O comando instala as bibliotecas pandas (para manipulação de dados) e openpyxl (para leitura e escrita de arquivos Excel .xlsx).